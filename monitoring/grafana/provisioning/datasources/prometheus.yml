apiVersion: 1

# Prometheus подключается автоматически при старте Grafana.
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
